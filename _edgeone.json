{
  "name": "myenglishai",
  "functions": {
    "dashscope-tts": {
      "runtime": "edge",
      "routes": ["/api/dashscope-tts"],
      "file": "functions/dashscope-tts/index.js",
      "env": {
        "DASHSCOPE_API_KEY": "{{DASHSCOPE_API_KEY}}"
      }
    }
  },
  "build": {
    "command": "npm run build",
    "outputDir": "dist"
  },
  "routes": [
    {
      "pattern": "/api/dashscope-tts",
      "function": "dashscope-tts"
    }
  ]
}
